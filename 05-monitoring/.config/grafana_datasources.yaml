apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    access: proxy
    url: db:5432
    user: postgres
    database: evidently
    basicAuth: false
    jsonData:
      sslmode: disable
    secureJsonData:
      password: postgres
